[package]
name = "ai-gateway"
version = "0.1.0"
edition = "2024"
description = "A production-oriented AI gateway built with Rust."
repository = "https://github.com/mezni/ai-gateway"

[dependencies]
anyhow = "1"
axum = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["io-util", "macros", "net", "rt-multi-thread", "signal", "sync", "time"] }

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
